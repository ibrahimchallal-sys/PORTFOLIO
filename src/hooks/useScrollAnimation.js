import { useEffect } from "react";

/**
 * Custom hook placeholder for scroll-triggered animations
 * @param {React.RefObject} elementRef - Reference to the element to animate
 * @param {Object} animationProps - Animation properties (currently disabled for performance)
 * @param {Object} scrollTriggerProps - Scroll properties (currently disabled for performance)
 */
const useScrollAnimation = (
  elementRef,
  animationProps,
  scrollTriggerProps = {}
) => {
  // This hook has been disabled to improve performance
  // Previously handled scroll-triggered animations
};

export default useScrollAnimation;
